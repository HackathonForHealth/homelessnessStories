<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/storyLayout"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="175dp"
    android:background="@drawable/story_title_background">

    <ViewFlipper
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/storyFlipper"
        android:layout_alignParentTop="true"/>
<!--
    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:id="@+id/storyButtonView">

        <Button
            android:text="@string/story_prev"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/storyPrev"
            android:onClick="onClickStoryFlipperPrev"
            android:layout_weight="1" />

        <Button
            android:text="@string/story_next"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/storyNext"
            android:onClick="onClickStoryFlipperNext"
            android:layout_weight="1" />
    </LinearLayout>
    -->
</RelativeLayout>